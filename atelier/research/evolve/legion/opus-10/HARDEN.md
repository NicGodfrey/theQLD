# Opus-10 · R20 scorecard (conductor fill — agent slot full)

**Pass.** `SCORECARD.json` has 20 rounds, `passed=20`, `gate=16`, `gates.fail_closed=true`. `test_evolve.Round17to20Meta` pins the file.

## Is v1 “non-dev finishes one loop” true?
**Mostly, in demo.** A non-dev can: create project → brief → pin SVG → quote → download → export zip, with visible plan and camera.

**Not yet Lovart-complete:** paid weave still needs a real API key (not ChatGPT Plus); SSE progress flushes after the sync run (UI can still feel blocked on a long image call); no 4-up pick-winner; upload is not vision-fed into the next weave automatically.

v1 gate (≥16/20 technical rounds) is met. Human “one paid loop” still waits on `OPENAI_API_KEY` / `GEMINI_API_KEY`.
