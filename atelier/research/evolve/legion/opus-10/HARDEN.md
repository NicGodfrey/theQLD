# Opus-10 · R20 scorecard — awaiting live agent

Live leftovers now in the tree:

- `SCORECARD.json` still reports technical 20/20 and gate 16
- It now also says `lovart_complete: false`, `paid_loop_proven: false`,
  `demo_loop: true`, and lists remaining product holes
- Per-round `note` fields record leftovers instead of pretending they closed

Verifier: one blocking `claude-opus-5-thinking-high-fast` agent. This is
the last round. Do not start a 21st.
