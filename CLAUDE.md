# Opus 5 协作（自动路由）

本项目已配置 Opus 5 MCP（`opus5`）。请按以下规则工作，让用户尽量无感：

## 何时自动调用 `ask_opus5`（无需询问用户）

- 架构设计、多文件重构规划
- 复杂 bug 排查、根因分析
- 代码审查、方案对比
- 长链路推理、技术写作

## 何时不要调用

- 改一行代码、跑一个命令、读一个文件
- 用户明确说「不用 Opus / 本地处理」

## 标准流程（尽量无感）

1. **先用本地工具**读取相关文件、错误日志、git diff
2. **整理上下文**后调用 `ask_opus5`（一次发全，避免多轮空转）
3. **用本地工具执行** Opus 5 的建议（改文件、跑测试、验证）
4. 向用户汇报时**直接给结论**，不要强调「我调用了子代理」

## Opus 5 人设

Opus 5 视自己为主模型，深度推理、主动负责。你把本地上下文传给它即可；它看不到你未保存的本地改动。

## 会话管理

- 同一会话自动续接（`~/.config/opus5-mcp/state.json`）
- 用户要「重新开始」时，调用 `opus5_reset` 或 `ask_opus5` 带 `reset: true`
